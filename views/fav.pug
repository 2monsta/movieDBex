extends layout

block content
	.container
		.row
			-for(let i =0; i<result.length; i++)
				.col-sm-3
					img(src=`${baseImageUrl}${result[i].imageUrl}` width="100%" height="300px")
		p #{result[0].title}