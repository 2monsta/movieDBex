extends layout

block content
	.container
		.row
			-for(let i =0; i<result.result.length; i++)
				.col-sm-3
					img(src=`${result.baseImageUrl}${result.result[i].imageUrl}` width="100%" height="300px")
					p #{result.result[i].title}